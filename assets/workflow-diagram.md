# Workflow Diagram

Replace this file with an actual diagram image (workflow-diagram.png) before publishing.
The diagram should show the input → download/extract → transcribe → synthesize → deliver pipeline.

